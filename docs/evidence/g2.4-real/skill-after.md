---
name: rsi-frontend-design
description: 根据页面需求和用户确认的设计偏好实现前端页面，保留功能、可访问性和响应式要求。
---

# 前端页面设计

先阅读当前项目与用户需求，使用已有技术栈和组件。新建页面时确认产物位置、页面用途、必需内容及交互；缺少会影响交付的信息时简短询问。只在用户授权的位置修改页面文件。

## 使用设计上下文

如果任务提供 RSI 冻结的设计上下文，使用其中的页面需求和有效偏好。它记录了本次、项目、个人偏好的合并结果；不要自行扩大作用范围。`skip`、`no_preference` 和 `unspecified` 表示没有可执行的审美要求，不要编造用户喜欢的风格。

用户最新明确要求优先；它与上下文、功能要求冲突时先指出具体冲突并确认。审美偏好不能免除功能、可访问性或用户指定的信息。没有上下文时，只问少量会改变设计选择的问题，允许用户跳过；不要宣称已经读取或保存了插件偏好。

## 杂志排版映射

用户确认“杂志排版”偏好时，按以下规则把抽象偏好转成具体布局与视觉选择，不要把它降级成一条口号。仅在本偏好在上下文中有效时使用；被覆盖或出现冲突时按“使用设计上下文”处理。

- 底色与文字：米白底色，正文与标题用近黑或纯黑文字；所有文案和界面文字默认黑色，不把灰字当作主要正文色。
- 强调色：只用少量橙红强调关键行动、短标注或当前状态；强调色不作为大面积背景或唯一信息载体。
- 禁忌：不使用渐变、阴影、圆角卡片网格或宽泛的卡片面板；内容的组织由文字层级、列结构和分隔线完成，而不是用方框逐个圈起来。
- 布局：从阅读顺序、主行动和必要信息出发决策。桌面首屏优先左右分栏，主标题在大字栏中形成醒目起点，副栏承担简介、CTA 或补充说明；移动端改为单栏，保持同样顺序，避免横向溢出。两栏只在桌面（约 900px 及以上）出现，窄屏恢复单栏。
- 标题与尺规：标题有编辑感，视觉分层来自字号、粗细、斜体或基线位置，而不是装饰。给标题足够留白，建立清晰的视觉锚点。
- 分隔与间距：用分隔线、留白和列边界切分版块，减少卡片和圆角；明确每个版块的阅读入口。杂志式标题可后于说明或 CTA 出现，只要视觉顺序清晰。
- 动效：动画与过渡遵循 prefers-reduced-motion，有动效也不得影响阅读顺序。
- 可访问性：保留语义结构、对比度、可见焦点、标签与键盘路径；上述视觉映射不得删除必需交互或隐藏键盘可达内容。

当用户只确认“现代”“简洁”等不涉及杂志的偏好时，不要套用本映射；杂志偏好只适用于确认了它的任务。若页面需求包含真实品牌或产品事实，应使用需求中的材料；对虚构演示内容采用明确、符合需求的虚构文案。

## 实现页面

1. 明确阅读顺序、主要行动和必要信息，再安排布局、间距、文字层级和强调色。将抽象偏好转成页面中的具体选择，不以模板风格替代用户要求。
2. 用语义结构、可读对比度、表单标签、键盘焦点和合理的窄屏布局。动效遵循减少动态效果设置。交互必须有可验证的行为，不能留下看似可点击却没有作用的按钮。
3. 复用可用资源。外部图片、字体或依赖需符合任务允许的网络与资源范围；没有产品事实时使用明确的虚构文案或占位内容，不编造客户、销量和评价。
4. 核对必需内容与交互，检查窄屏、长文本、键盘操作和动效设置。说明实际执行的验证及其限制；没有查看页面时不声称视觉验收通过。

## 交付与反馈

简洁说明产物位置、关键设计选择、验证结果和仍需确认的点。用户反馈只作为当前修改依据；持久保存偏好由 RSI 界面中的明确选择完成。

不要自行修改本 Skill、授权、预算、评测器或有效版本。新的 Skill 只能由独立的 RSI 优化任务在获准范围内形成候选；页面符合偏好不等于 Skill 能力已提升。
