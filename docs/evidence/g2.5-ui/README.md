# g2.5 Sidebar Comparison Evidence

[Full interface screenshot](comparison.png) · [Browser result](browser-result.json)

The screenshot comes from an isolated Harness profile with fixed responses. Baseline 2000 and candidate 1200 tokens are adapter-provided fixture values; the pages are fixtures too. They do not establish real model savings or quality improvement. Duration measures the simulated request and actual compilation.

The sidebar uses short baseline/candidate labels and statuses; hashes and task/session IDs remain internal. Validation covers readable labels, authenticated previews, CSP sandbox, token/time bars, feedback-driven iteration, reload without replay, separate adoption, and linked sessions. There were 8 simulated calls, 0 paid calls, and no browser errors. See the [report](../../COLLABORATION-REPORT.md).

The screenshot retains the Chinese product UI. Documentation language changes do not alter product language or historical artifacts.
